name: First workflow

on: [push,fork]

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: welcom message
              uses: actions/checkout@v6
            - name: show folders
              run: ls
            - name: catting 
              run: cat README.MD
            